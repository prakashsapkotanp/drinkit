services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: drinkin
      POSTGRES_PASSWORD: drinkin
      POSTGRES_DB: drinkin
    ports:
      - "5432:5432"
    volumes:
      - drinkin_pgdata:/var/lib/postgresql/data

volumes:
  drinkin_pgdata:
